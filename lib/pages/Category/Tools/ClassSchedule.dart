import 'package:flutter/material.dart';

class ClassSchedule extends StatelessWidget {
  const ClassSchedule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Class Schedule UI'),
    );
  }
}
