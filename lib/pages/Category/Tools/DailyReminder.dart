import 'package:flutter/material.dart';

class DailyReminder extends StatelessWidget {
  const DailyReminder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Daily Reminder UI'),
    );
  }
}
